diff --git a/src/common/mathutil.h b/src/common/mathutil.h
index bf98acd56..94e6e3e88 100644
--- a/src/common/mathutil.h
+++ b/src/common/mathutil.h
@@ -1113,7 +1113,7 @@ inline int BitCount(uint64_t bits)
 #    endif  // defined(_M_IX86) || defined(_M_X64)
 #endif      // defined(_MSC_VER) && !defined(__clang__)
 
-#if defined(ANGLE_PLATFORM_POSIX) || defined(__clang__) || defined(__GNUC__)
+#if defined(ANGLE_PLATFORM_POSIX) || defined(__clang__) || defined(__GNUC__) || defined(__MINGW32__)
 inline int BitCount(uint32_t bits)
 {
     return __builtin_popcount(bits);
@@ -1123,7 +1123,7 @@ inline int BitCount(uint64_t bits)
 {
     return __builtin_popcountll(bits);
 }
-#endif  // defined(ANGLE_PLATFORM_POSIX) || defined(__clang__) || defined(__GNUC__)
+#endif  // defined(ANGLE_PLATFORM_POSIX) || defined(__clang__) || defined(__GNUC__) || defined(__MINGW32__)
 
 inline int BitCount(uint8_t bits)
 {
