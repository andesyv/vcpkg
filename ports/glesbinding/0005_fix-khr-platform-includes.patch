diff --git a/CMakeLists.txt b/CMakeLists.txt
index d998de7..a6e4442 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -196,6 +196,7 @@ install(FILES "${PROJECT_BINARY_DIR}/VERSION" DESTINATION ${INSTALL_ROOT} COMPON
 
 # Install cmake find script for the project
 install(FILES ${META_PROJECT_NAME}-config.cmake DESTINATION ${INSTALL_ROOT} COMPONENT dev)
+install(FILES cmake/FindKHR.cmake DESTINATION ${INSTALL_ROOT}/cmake COMPONENT dev)
 
 # Install the project meta files
 install(FILES AUTHORS   DESTINATION ${INSTALL_ROOT} COMPONENT runtime)
diff --git a/cmake/FindKHR.cmake b/cmake/FindKHR.cmake
index e77cbf7..0457f14 100644
--- a/cmake/FindKHR.cmake
+++ b/cmake/FindKHR.cmake
@@ -20,11 +20,15 @@ find_path(KHR_INCLUDE_DIR KHR/khrplatform.h
 
     DOC "The directory where KHR/khrplatform.h resides")
 
-add_library(KHR::KHR INTERFACE IMPORTED)
+if(KHR_INCLUDE_DIR)
 
-set_target_properties(KHR::KHR PROPERTIES
-  INTERFACE_INCLUDE_DIRECTORIES "${KHR_INCLUDE_DIR}"
-)
+    add_library(KHR::KHR INTERFACE IMPORTED)
+
+    set_target_properties(KHR::KHR PROPERTIES
+      INTERFACE_INCLUDE_DIRECTORIES "${KHR_INCLUDE_DIR}"
+    )
+
+endif()
 
 find_package_handle_standard_args(KHR REQUIRED_VARS KHR_INCLUDE_DIR)
 mark_as_advanced(KHR_INCLUDE_DIR KHR_LIBRARY)
diff --git a/glesbinding-config.cmake b/glesbinding-config.cmake
index 49338ab..bc51ba5 100644
--- a/glesbinding-config.cmake
+++ b/glesbinding-config.cmake
@@ -4,9 +4,17 @@
 # 
 # Please adjust the list of submodules to search for.
 
+include(CMakeFindDependencyMacro)
+
+if (NOT TARGET "KHR::KHR")
+    message(STATUS "${CMAKE_CURRENT_LIST_DIR}/cmake")
+    list(PREPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/cmake")
+    find_package(KHR QUIET)
+endif ()
 
 # List of modules
 set(MODULE_NAMES
+    KHRplatform
     glesbinding
     glesbinding-aux
 )
